import { StyleSheet, Text, View } from "react-native";
import React from "react";
import Tabs from "../../screens/help/Tabs";

const Footer = () => {
  return <Tabs />;
};

export default Footer;

const styles = StyleSheet.create({});
